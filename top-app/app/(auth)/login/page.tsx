import { Paragraph } from "@/components";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "About",
};

export default function Login() {
  return (
    <div>
      <Paragraph>Логирование</Paragraph>
    </div>
  );
}
